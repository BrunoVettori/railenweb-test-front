export type CompileAstFn = (compileOpts: {
    code: string;
    filename: string;
    getBabelConfig: () => {
        plugins: Array<any>;
    };
}) => Promise<{
    code: string;
    map: any;
}>;
export declare function compileAst(makeOpts: {
    root: string;
}): (opts: {
    code: string;
    filename: string;
    getBabelConfig: () => {
        plugins: Array<any>;
    };
}) => Promise<{
    code: string;
    map: any;
}>;
